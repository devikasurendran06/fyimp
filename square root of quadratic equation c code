#include <stdio.h>
#include <math.h>
int main()
{
    float a,b,c,d,r1,r2;
    printf("enter a,b,c: \n");
    scanf("%f %f %f",&a,&b,&c);
    d=(b*b)-(4*a*c);
    r1=(-b+sqrt(d))/(2*a);
    r2=(-b-sqrt(d))/(2*a);
    printf("root1=%d",r1);
    printf("root2=%d",r2);
}
